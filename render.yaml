services:
  - type: web
    name: genie-frontend
    root: my-saas-platform/apps/Dashboard-Dark
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
