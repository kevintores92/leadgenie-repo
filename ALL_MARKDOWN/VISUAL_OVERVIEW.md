# 🎨 Visual System Overview

## Complete Billing System Architecture

```
┌────────────────────────────────────────────────────────────────────┐
│                         FRONTEND (Next.js)                         │
│                                                                    │
│  BillingDashboard.tsx         BillingGuard.tsx      /billing page  │
│  ├─ Wallet Balance            ├─ Overlay Wrapper    ├─ History    │
│  ├─ Subscription Status       ├─ Block Message      ├─ Status     │
│  ├─ Top-up Buttons            └─ Show Reason        └─ Info       │
│  └─ Subscribe Button                                               │
│            ↓                         ↓                        ↓    │
├────────────────────────────────────────────────────────────────────┤
│                       API ROUTES (Next.js)                         │
│                                                                    │
│  /api/billing/status                                               │
│  └─ GET → { wallet, subscription, canSend }                        │
│                                                                    │
│  /api/billing/create-subscription                                  │
│  └─ POST → { approvalLink }  ─→ PayPal                            │
│                                                                    │
│  /api/billing/create-topup                                         │
│  └─ POST → { checkoutLink }  ─→ PayPal                            │
│                                                                    │
│  /api/webhooks/paypal                                              │
│  └─ POST ← [PayPal events]                                         │
│                                                                    │
│  /api/webhooks/twilio                                              │
│  └─ POST ← [Twilio delivery status]                               │
│            ↓                     ↓                           ↓    │
├────────────────────────────────────────────────────────────────────┤
│                    BACKEND SERVICES (TypeScript)                   │
│                                                                    │
│  BillingService              PayPalService                         │
│  ├─ getOrCreateWallet()     ├─ createSubscription()              │
│  ├─ canSendMessage()        ├─ createCheckout()                  │
│  ├─ debitWallet()           ├─ capturePayment()                  │
│  ├─ creditWallet()          └─ verifySignature()                 │
│  ├─ freezeWallet()                                                │
│  ├─ getSubscription()       TwilioSubaccountService              │
│  └─ calculateFinalCost()    ├─ suspendSubaccount()              │
│                             └─ activateSubaccount()              │
│            ↓                     ↓                           ↓    │
├────────────────────────────────────────────────────────────────────┤
│                    DATABASE (PostgreSQL/Prisma)                    │
│                                                                    │
│  organization_wallet                organization_subscription     │
│  ├─ balance_cents              ├─ provider (PAYPAL/STRIPE)       │
│  ├─ is_frozen                  ├─ provider_sub_id                │
│  └─ organization_id            ├─ status (ACTIVE/PAST_DUE)       │
│                                └─ current_period_end              │
│                                                                    │
│  wallet_transaction            webhook_event                      │
│  ├─ type (DEBIT/CREDIT)        ├─ provider                       │
│  ├─ amount_cents               ├─ external_id (prevent dup)      │
│  ├─ reference_id               └─ event_type                      │
│  └─ organization_id                                               │
│            ↓                     ↓                           ↓    │
├────────────────────────────────────────────────────────────────────┤
│                    EXTERNAL SERVICES                               │
│                                                                    │
│  PayPal                        Twilio                              │
│  ├─ Create Subscription        ├─ Send SMS                        │
│  ├─ Create Checkout            ├─ Manage Subaccounts             │
│  ├─ Process Payments           └─ Report Status                   │
│  └─ Send Webhooks                                                 │
│            ↓                     ↓                           ↓    │
├────────────────────────────────────────────────────────────────────┤
│                    WORKER SERVICES (Node.js)                       │
│                                                                    │
│  campaignSender.js            billingCheck.js                      │
│  ├─ Get jobs from queue       ├─ checkBillingBeforeSend()        │
│  ├─ Call billingCheck()       ├─ debitWalletForMessage()         │
│  ├─ Send SMS via Twilio       └─ calculateCostWithMarkup()       │
│  └─ Log results                                                    │
│                                                                    │
│            ↓                     ↓                           ↓    │
└────────────────────────────────────────────────────────────────────┘
```

## Message Send Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│ User clicks "Send Campaign"                                         │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Frontend creates campaign job, queues to BullMQ                    │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Worker (campaignSender.js) gets job from queue                     │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│ checkBillingBeforeSend() validates:                                │
│  1. subscription.status === ACTIVE?                                │
│  2. wallet.isFrozen === false?                                     │
│  3. wallet.balance ≥ estimatedCost?                                │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
                      ┌────┴────┐
                      │          │
              YES ←───┴───→ NO (BLOCK)
              ↓                  ↓
         ┌────────────┐  ┌──────────────┐
         │Send SMS    │  │Log reason    │
         │via Twilio  │  │Retry later   │
         └────┬───────┘  └──────────────┘
              ↓
         ┌────────────────────────┐
         │Twilio delivers message │
         │(or fails)              │
         └────┬───────────────────┘
              ↓
         ┌────────────────────────────────────────────┐
         │Twilio webhook fires: delivery status      │
         │POST /api/webhooks/twilio                  │
         └────┬───────────────────────────────────────┘
              ↓
              ├─ Status = "delivered"
              │  ├─ Calculate cost = Twilio + 30% markup
              │  ├─ Debit wallet
              │  └─ Create transaction record
              │
              └─ Status = "failed" or other
                 └─ No charge
                 └─ No wallet debit
                 └─ Release hold
                 ↓
         ┌──────────────────────────┐
         │BillingDashboard updates  │
         │Shows new wallet balance  │
         └──────────────────────────┘
```

## Subscription Lifecycle

```
┌────────────────────────────────────────────────────────────────────┐
│ User clicks "Subscribe"                                            │
└──────────────────────┬───────────────────────────────────────────┘
                       ↓
        ┌──────────────────────────────┐
        │POST /api/billing/           │
        │create-subscription          │
        └──────────┬───────────────────┘
                   ↓
        ┌──────────────────────────────┐
        │Create PayPal Subscription    │
        │Status: PAST_DUE (initial)    │
        └──────────┬───────────────────┘
                   ↓
        ┌──────────────────────────────┐
        │Redirect to PayPal approval   │
        │User approves payment method  │
        └──────────┬───────────────────┘
                   ↓
                   ├─ APPROVED
                   │  └─→ PayPal creates subscription
                   │      └─→ webhook fired
                   │
                   └─ DECLINED
                      └─→ User sees error

        ┌────────────────────────────────────────────┐
        │PayPal Webhook:                            │
        │BILLING.SUBSCRIPTION.ACTIVATED             │
        └──────────┬─────────────────────────────────┘
                   ↓
        ┌────────────────────────────────────────────┐
        │Update subscription.status = ACTIVE        │
        └──────────┬─────────────────────────────────┘
                   ↓
        ┌────────────────────────────────────────────┐
        │Call TwilioSubaccountService               │
        │.activateSubaccount()                      │
        │(Twilio subaccount status = 'active')      │
        └──────────┬─────────────────────────────────┘
                   ↓
        ┌────────────────────────────────────────────┐
        │Unfreeze wallet                            │
        │(wallet.isFrozen = false)                  │
        └──────────┬─────────────────────────────────┘
                   ↓
        ┌────────────────────────────────────────────┐
        │✅ Organization can send messages!         │
        │✅ BillingDashboard shows ACTIVE status    │
        └────────────────────────────────────────────┘

        ═════════════════════════════════════════════

        (Monthly: Payment succeeds → subscription continues)
        (Or: Payment fails → different webhook)

        ┌─────────────────────────────────────────┐
        │Payment fails / Subscription expires     │
        │PayPal Webhook:                          │
        │BILLING.SUBSCRIPTION.SUSPENDED           │
        └──────────┬────────────────────────────┘
                   ↓
        ┌────────────────────────────────────────┐
        │Update subscription.status = PAST_DUE   │
        └──────────┬───────────────────────────┘
                   ↓
        ┌────────────────────────────────────────┐
        │Call TwilioSubaccountService            │
        │.suspendSubaccount()                    │
        │(Twilio subaccount status = 'suspended')│
        └──────────┬───────────────────────────┘
                   ↓
        ┌────────────────────────────────────────┐
        │Freeze wallet                           │
        │(wallet.isFrozen = true)                │
        └──────────┬───────────────────────────┘
                   ↓
        ┌────────────────────────────────────────┐
        │❌ Message sends are blocked!           │
        │❌ BillingDashboard shows PAST_DUE     │
        │❌ Twilio not charging (suspended)      │
        └────────────────────────────────────────┘

        (User resolves payment → subscription reactivates)
        (Or: Subscription expires → cancellation webhook)
```

## Wallet Top-Up Flow

```
┌──────────────────────────────────────┐
│User: "Top up wallet with $50"        │
└──────────────┬──────────────────────┘
               ↓
        ┌──────────────────────────────┐
        │POST /api/billing/create-topup│
        │amount: $50                   │
        └──────────┬───────────────────┘
                   ↓
        ┌──────────────────────────────┐
        │Create PayPal Order           │
        │Store org_id in metadata      │
        └──────────┬───────────────────┘
                   ↓
        ┌──────────────────────────────┐
        │Redirect to PayPal Checkout   │
        │User enters payment method    │
        └──────────┬───────────────────┘
                   ↓
                   ├─ APPROVED
                   │  └─→ PayPal processes payment
                   │      └─→ webhook fired
                   │
                   └─ DECLINED
                      └─→ User sees error

        ┌────────────────────────────────────────┐
        │PayPal Webhook:                        │
        │PAYMENT.SALE.COMPLETED                 │
        └──────────┬─────────────────────────────┘
                   ↓
        ┌────────────────────────────────────────┐
        │Extract org_id from order metadata      │
        │Calculate cents: $50 → 5000¢            │
        └──────────┬─────────────────────────────┘
                   ↓
        ┌────────────────────────────────────────┐
        │wallet.balance += 5000¢                 │
        │Create PAYMENT_TOPUP transaction        │
        └──────────┬─────────────────────────────┘
                   ↓
        ┌────────────────────────────────────────┐
        │✅ Wallet credited immediately!        │
        │✅ BillingDashboard shows new balance  │
        │✅ User can send messages               │
        └────────────────────────────────────────┘
```

## Safety Guarantees

```
RULE #1: No negative wallet balances
┌────────────────────────────────────────────┐
│ wallet.balance < estimatedCost?            │
│ YES → BLOCK SEND                           │
│ NO  → ALLOW SEND                           │
└────────────────────────────────────────────┘

RULE #2: No sending without both layers
┌────────────────────────────────────────────┐
│ subscription.status !== ACTIVE?            │
│ YES → BLOCK SEND                           │
│ NO  → Check wallet...                      │
│                                             │
│ wallet.isFrozen === true?                  │
│ YES → BLOCK SEND                           │
│ NO  → Check balance...                     │
│                                             │
│ wallet.balance < estimatedCost?            │
│ YES → BLOCK SEND                           │
│ NO  → ALLOW SEND ✓                         │
└────────────────────────────────────────────┘

RULE #3: No Twilio charges when inactive
┌────────────────────────────────────────────┐
│ subscription.status !== ACTIVE?            │
│ YES → suspend Twilio subaccount            │
│       (status = 'suspended')               │
│       (no API calls allowed)               │
│       (no charges accrue) ✓                │
│ NO  → activate Twilio subaccount           │
│       (status = 'active')                  │
│       (API calls allowed)                  │
│       (charges accrue)                     │
└────────────────────────────────────────────┘

RULE #4: No double-charging on webhook replay
┌────────────────────────────────────────────┐
│ webhook_event.external_id exists?          │
│ YES → Duplicate detected!                  │
│       Log as duplicate                     │
│       SKIP processing ✓                    │
│ NO  → New event                            │
│       INSERT into webhook_event            │
│       Process event                        │
│       Update wallet/subscription           │
└────────────────────────────────────────────┘

RULE #5: Wallet freezing on non-payment
┌────────────────────────────────────────────┐
│ subscription.status changes to PAST_DUE    │
│ ├─ Set wallet.isFrozen = true              │
│ ├─ User can't send                         │
│ └─ Protects against accidental sends ✓     │
│                                             │
│ subscription.status changes to ACTIVE      │
│ ├─ Set wallet.isFrozen = false             │
│ ├─ User can send (if balance sufficient)   │
│ └─ Resume normal operations ✓              │
└────────────────────────────────────────────┘
```

## Code Organization by Feature

```
BILLING FEATURE ORGANIZATION
════════════════════════════════════════════

Backend Services (Logic)
  backend-api/src/services/
  ├── billingService.ts (10 methods)
  ├── paypalService.ts (7 methods)
  └── twilioSubaccountService.ts (4 methods)

API Routes (Communication)
  frontend/pages/api/
  ├── billing/
  │   ├── status.ts (GET)
  │   ├── create-subscription.ts (POST)
  │   └── create-topup.ts (POST)
  └── webhooks/
      ├── paypal.ts (POST)
      └── twilio.ts (POST)

UI Components (Display)
  frontend/components/
  ├── BillingDashboard.tsx
  └── BillingGuard.tsx
  
  frontend/pages/app/
  └── billing.tsx

Worker Integration (Enforcement)
  worker-services/src/lib/
  └── billingCheck.js

Database (Storage)
  */prisma/
  ├── schema.prisma (4 models)
  └── migrations/add_billing_system/
      └── migration.sql

This organization keeps billing features isolated
while connecting to shared infrastructure.
```

## Deployment Timeline

```
BEFORE DEPLOYMENT        DEPLOYMENT           AFTER DEPLOYMENT
═════════════════════    ════════════════     ════════════════════

All code written ✓       1. Database          Production testing
All schemas ready ✓         migration         Monitoring active
All docs complete ✓         (5 min)           Team trained

                         2. Env variables
                            (10 min)

                         3. PayPal setup
                            (10 min)

                         4. Webhook setup
                            (5 min)

                         5. Worker
                            integration
                            (5 min)

                         6. Testing
                            (10 min)

                         ─────────────
                         Total: 45 min

                                              Day 1: Monitor logs
                                              Day 2: Alert setup
                                              Week 1: Verify all flows
```

## File Count Summary

```
NEW FILES CREATED
═════════════════════════════════

Services:      3 files
API Routes:    5 files
Components:    3 files
Worker:        1 file
Database:      1 file
Docs:          7 files
               ───────
TOTAL:        20 files


MODIFIED FILES
═════════════════════════════════

Schemas:       3 files
Contact code:  3 files
               ───────
TOTAL:         6 files


FILES MODIFIED + CREATED: 26 files total
TOTAL CODE LINES: ~1,500
TOTAL DOC LINES: ~2,000
```

---

This visual overview should help you understand:
✅ System architecture
✅ Message and payment flows
✅ Safety guarantees
✅ Code organization
✅ Deployment process
✅ Implementation scope
