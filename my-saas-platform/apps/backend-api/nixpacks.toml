[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npx prisma generate --schema=./prisma/schema.prisma']

[start]
cmd = 'npx prisma migrate deploy --schema=./prisma/schema.prisma && node src/server.js'
